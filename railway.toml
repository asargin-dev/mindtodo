[build]
builder = "nixpacks"
buildCommand = "npm install && npm run test:run && npm run build"

[deploy]
startCommand = "npm run preview -- --host 0.0.0.0 --port $PORT"

[build.env]
NIXPACKS_NODE_VERSION = "20"
NO_CACHE = "1"
